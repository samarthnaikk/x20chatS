# Data Directory

This directory is used for storing application data.

## Subdirectories

- `received_files/` - Files received through P2P file transfers are saved here

## Notes

- The `received_files/` directory is automatically created when needed
- Received files are saved with their original filenames
- This directory and its contents are excluded from version control (see `.gitignore`)
